{
  "name": "demo03",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --outDir build",
    "rollup": "rollup -f iife -i build/root.js -o build/bundle.js",
    "start": "npm run build && npm run rollup"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inferno": "^1.2.2",
    "inferno-create-element": "^1.3.0-rc.3"
  },
  "devDependencies": {
    "rollup": "^0.41.4",
    "typescript": "^2.1.6"
  }
}
